# Local-dev only. Points the data fetcher at the bundled fixture under public/
# so `npm run dev` works offline. Production builds use the default jsDelivr
# URL defined in src/constants.ts.
VITE_DATA_BASE=/go-cam-model-status/fixture
